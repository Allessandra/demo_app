<%= form_for(@post) do |f|%>
	<%= render 'shared/error_messages', object: @post%>
	<div class="field">
		
		<div class="row">
			<div class="col-md-6">
				<span class="title"><%=f.text_area :title ,placeholder: "Title"%></span>
			</div>
		</div>
		<div class="row">
			<div class="col-md-6">
				<span class="title"><%=f.text_area :content, placeholder: "Compose new post...."%></span>
			</div>
		</div>
		
		
		
	</div>
	<%= f.submit "Post", class:"btn btn-large btn-primary"%>
<%end%>
