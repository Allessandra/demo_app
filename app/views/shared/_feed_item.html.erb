<li id="<%= feed_item.id%>">
	<div class="row">
		<div class="col-md-3">
			<%= link_to gravatar_for(feed_item.user),feed_item.user%>
			<span class="user">
				<%=link_to feed_item.user.name,feed_item.user%>
			</span>
		</div>
		<div class="col-md-8">
			<div class ="row">
				<div class="col-md-3">
					<h3><span class="title"><%= feed_item.title%></span></h3>
				</div>
			</div>
		<div class ="row">
			<div class="col-md-8">
				<span class="content"><%= truncate(feed_item.content,:length=>50)%></span>
			</div>
		</div>
		<div class="row">
			<div class="col-md-5">
				<span class="timestamp">
					Posted <%= time_ago_in_words(feed_item.created_at)%> ago.
				</span>
			</div>
		<div class="col-md-3">
		<% if current_user?(feed_item.user)%>
			<%= link_to "delete",feed_item,method: :delete, data: {confirm: "You sure?"}  %>
			<%= link_to "edit",edit_post_path(feed_item) %>
			<%= link_to "details",post_path(feed_item)%>
		<% end%>
		</div>
	</div>
</div>
</li>
